# Adaptation notes

This derivative changes an implementation-oriented observability method into a private coverage worksheet. It cannot access telemetry or perform collection, alerting, sampling, configuration, deployment, messaging, ticketing, publishing, or eval-set writes.
